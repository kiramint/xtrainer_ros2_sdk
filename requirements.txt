# =============================================================================
# XTrainer conda env (xtrainer_env) requirements
#
# 使用方法:
#   conda activate xtrainer_env
#   pip install -r requirements.txt
#
# 注意:
#   - numpy 必须为 1.26.4 (与 ROS2 Jazzy 系统包 python3-numpy 1.26.4 ABI 一致,
#     否则 MoveItPy / rclpy 的 C 扩展运行时 SIGSEGV)
#   - 所有依赖 numpy 的科学计算库 (scipy/opencv/sklearn/matplotlib) 必须成套
#     选用兼容 numpy 1.x 的版本, 单独升级其中任何一个会触发 ABI 错配
#   - ROS2/MoveItPy 编译时仍应先 `conda deactivate`, 用 /usr/bin/python3 colcon build,
#     仅运行时用此 conda env
# =============================================================================

# -----------------------------------------------------------------------------
# ROS2 工具链 (与系统 apt 装的 colcon 一致)
# -----------------------------------------------------------------------------
colcon-common-extensions
pyyaml
lark

# -----------------------------------------------------------------------------
# 基础数值/图像库 (版本与 Ubuntu 24.04 系统包对齐, 强制 1.x ABI)
# -----------------------------------------------------------------------------
numpy==1.26.4                  # 必须为此版本, 与 python3-numpy 1.26.4+ds-6ubuntu1 ABI 一致
opencv-contrib-python~=4.6.0   # 兼容 numpy 1.26.4

# -----------------------------------------------------------------------------
# GroundingDINO & SAM2 (检测/分割)
# -----------------------------------------------------------------------------
supervision==0.29.1
transformers==4.44.2
yapf==0.43.0
pycocotools==2.0.11
timm==1.0.28

# -----------------------------------------------------------------------------
# GraspNet 抓取 (3D 点云 + 经典 ML)
# -----------------------------------------------------------------------------
scipy==1.13.1                  # scipy 1.13.x 是最后兼容 numpy 1.x 的系列 (>=1.14 要求 numpy 2.x)
open3d==0.19.0
scikit-learn==1.9.0
matplotlib==3.11.1
